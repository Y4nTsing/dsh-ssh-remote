{
  "name": "ssh-remote-preset",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local dependency root for the ssh-remote preset plugin (ssh2).",
  "dependencies": {
    "ssh2": "^1.17.0"
  }
}
