name: SSH Remote Mode
description: 远程 Linux 专用模式：会话开始时通过 ask_user_question 收集 SSH 主机/端口/账号/密码并建立连接，之后所有命令（ssh_bash）与文件读写（ssh_read/ssh_write/ssh_edit）全部在远端服务器执行，文件经 SFTP 上传下载且本地路径限制在会话工作区内；不挂载任何本地 shell 或本地文件工具。
